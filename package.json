{
  "name": "grant_compass",
  "version": "1.0.0",
  "private": true,
  "description": "Empty package.json needed for Heroku build",
  "scripts": {
    "postinstall": "if [ $CLIENT_ENV ]; then npm run postinstall-client;",
    "postinstall-client": "cd frontend && npm install"
  }
}
